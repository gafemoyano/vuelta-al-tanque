---
layout: layouts/default
title: Inicio
path: home
---

<section class="flex items-center justify-center w-full py-8 mx-auto bg-white">  
  <div class="flex flex-col items-center justify-center w-full max-w-3xl gap-8 px-4 mx-auto sm:px-2 sm:flex-row sm:gap-16">
    <p class="font-serif leading-none w-128" style="font-size: clamp(2.25rem,6vw,3rem);">
      {{subtitulo | safe}}
    </p>
    <div  class="inline-flex items-center justify-center w-full sm:w-[360px]">
      <img src="{{image}}" alt="bombillo en blanco y negro" class="w-auto">
    </div>
  </div>
</section>

<section class="container mx-auto mb-16">
  <h2 class="mt-12 text-3xl font-light text-center lg:text-4xl">Últimos artículos</h2>
  <hr class="w-16 mx-auto my-8 border-b-2 border-accent">
  {% set postslist = collections.post %}
  {% include 'partials/post-list.njk'%}
</section>