---
layout: layouts/default
title: Inicio
path: home
---

<section class="flex items-center justify-center w-full py-16 mx-auto bg-white border-b-2 border-black">  
  <div class="w-full mx-auto flex gap-8 md:gap-16 max-w-screen-md items-center justify-center">
    <p class="w-128 leading-none font-display" style="font-size: clamp(3.25rem,6vw,4rem);">
      {{subtitulo | safe}}
    </p>
    <div  class="inline-flex items-center justify-center w-full md:w-[640px]">
      <img src="{{image}}" alt="bombillo en blanco y negro" class="w-auto">
    </div>
  </div>
</section>

<section class="container mx-auto mb-16">
  <h2 class="mt-12 text-2xl text-center lg:text-5xl">Últimos artículos</h2>

  {% set postslist = collections.post %}
  {% include 'partials/post-list.njk'%}
</section>