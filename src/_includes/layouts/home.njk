---
layout: layouts/default
title: Inicio
path: home
---

<section class="flex flex-col items-center justify-center w-full py-16 mx-auto bg-secondary">
  {# <div  class="flex items-center justify-center">
    <img src="/static/img/absurd/01.png" alt="bombillo en blanco y negro" class="w-auto h-48">
  </div> #}
  <div>
    <h1 class="leading-none text-center font-display text-text" style="font-size: clamp(3.25rem,6vw,4rem);">
      {{encabezado}}
    </h1>

    <p class="max-w-md mt-6 text-xl font-medium text-center text-text">
      {{subtitulo}}
    </p>

    <div class="flex items-center justify-center mt-12 space-x-4">
    {% for item in navigation.items %}
      <a class="inline-block text-lg font-medium hover:underline text-accent" href="{{ item.url }}">{{ item.text }}</a>
    {% endfor %}
    </div>
  </div>
</section>

<section class="container mx-auto mb-16">
  <h2 class="mt-12 text-2xl text-center lg:text-5xl">Últimos artículos</h2>

  {% set postslist = collections.post %}
  {% include 'partials/post-list.njk'%}
</section>