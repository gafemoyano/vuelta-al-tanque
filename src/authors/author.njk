---
layout: layouts/default.njk
pagination:
  data: collections.authors
  size: 1
  alias: author
permalink: "autores/{{ author.data.slug }}/index.html"
---

<h1>Artículos by {{ author.data.name }}</h1>
<ul>

  {% for post in collections.post | filterPostsByAuthor(author.data.name) %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </li>
  {% endfor %}
</ul>